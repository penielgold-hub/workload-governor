# ── Backend ────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3000

# ── PostgreSQL ─────────────────────────────────────────────────────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=workload_governor
POSTGRES_PORT=5432
# Full connection string (constructed automatically by docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/workload_governor

# ── Database connection pool (issue #561) ──────────────────────────────────
# DB_POOL_MIN: minimum connections kept alive in the pool (default: 2)
DB_POOL_MIN=2
# DB_POOL_MAX: maximum connections allowed in the pool (default: 10)
DB_POOL_MAX=10
# DB_IDLE_TIMEOUT: ms before an idle connection is closed (default: 30000)
DB_IDLE_TIMEOUT=30000
# DB_CONNECTION_TIMEOUT: ms to wait for a connection before erroring (default: 5000)
DB_CONNECTION_TIMEOUT=5000

# ── Redis ──────────────────────────────────────────────────────────────────
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379

# ── Stellar / Soroban RPC ──────────────────────────────────────────────────
# Local quickstart node — change to testnet/mainnet URL for non-local envs
STELLAR_PORT=8000
SOROBAN_RPC_URL=http://localhost:8000/soroban/rpc
STELLAR_NETWORK_PASSPHRASE=Standalone Network ; February 2017
# Deployed contract ID (leave empty until first deploy)
CONTRACT_ID=

# ── Metrics ────────────────────────────────────────────────────────────────
# Security token for the /metrics endpoint (optional).
# If set, requests must include "Authorization: Bearer <token>" header.
# METRICS_TOKEN=your_metrics_token_here

# ── Event Backfill ─────────────────────────────────────────────────────────
# Backfill historical contract events from Horizon using:
#   npm run backfill -- --from-ledger=1000 --to-ledger=2000
#   npm run backfill -- --from-ledger=1000 --to-ledger=2000 --resume
# Flags:
#   --from-ledger=N   Starting ledger sequence (required)
#   --to-ledger=M     Ending ledger sequence (required)
#   --resume          Resume from last processed ledger (idempotent)
#   --batch-size=N    Events per RPC call (default: 200)

# ── pgAdmin (dev convenience, not needed in CI) ────────────────────────────
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ── AWS (only required for infra / CI jobs) ────────────────────────────────
# AWS_REGION=us-east-1
# AWS_ROLE_ARN=arn:aws:iam::123456789012:role/github-actions
# RDS_DB_INSTANCE_ID=workload-governor-prod
# SNS_ALERT_TOPIC_ARN=arn:aws:sns:us-east-1:123456789012:devops-alerts
